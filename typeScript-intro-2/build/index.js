const getName = ({ firstname, lastname }) => {
    let fullname = firstname + lastname;
    if (!lastname) {
        fullname = firstname;
        console.log(fullname);
    }
    else {
        console.log(fullname);
    }
};
let arr;
const PhoneBook = (PersonDetailsObj) => {
    arr.push(PersonDetailsObj);
};
//# sourceMappingURL=index.js.map